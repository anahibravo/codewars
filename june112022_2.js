function rowSumOddNumbers(n) {
	return n * n * n
}

/* This first attempt I created this but I think it wasn't enough. 

//create a variable that hold the value of the sum of each row
  const sum = 0
  //iterate through each element then add them
  for (let i = 1; i > n.length; i++){
    sum += n[i];
  }
  return sum

// I also was thinking of Math.pow
function rowSumOddNumbers(n) {
  return Math.pow(n, 3);
}

  */