String.prototype.toAlternatingCase = function () {
  
    let upper = this[i].spli('').toUpperCase()
    let lower = this[i].split('').toLowerCase()
    if ( this[i] === upper){
      return lower.join('')
    } else  upper.join('')
    
  }