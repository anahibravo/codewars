function billboard(name, price = 30){
    let cost = 0
    for (let i = 0; i < name.length; i++){
      cost += price 
      
    }
      return cost
    } 
    
    
    /*
    I can't use the * hahaa but would this have worked otherwise?
    const value = 0
      let cost = name.reduce((acc,c) => acc * c, value)
    console.log (cost)
    let sum = nums.reduce( (acc, c) => acc + c , 0)
    */